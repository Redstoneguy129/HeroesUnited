/*package xyz.heroesunited.heroesunited.common.objects.items;

import net.minecraft.entity.player.PlayerEntity;
import net.minecraft.item.Item;
import net.minecraft.item.ItemStack;
import net.minecraft.util.ResourceLocation;
import xyz.heroesunited.heroesunited.HeroesUnited;

public class AccesoireItem extends Item implements IAccessoire {

    private final int slot;

    public AccesoireItem(Properties properties, int slot) {
        super(properties);
        this.slot = slot;
    }

    @Override
    public ResourceLocation getTexture(ItemStack stack, PlayerEntity entity) {
        return new ResourceLocation(HeroesUnited.MODID, "textures/gui/accessories_gui.png");
    }

    @Override
    public int getSlot() {
        return slot;
    }
}
*/